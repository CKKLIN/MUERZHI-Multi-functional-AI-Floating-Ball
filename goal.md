# MUERZHI 万能AI 悬浮球 —— 执行目标文件（goal.md）

> 本文件供 `/goal` 命令逐项执行。每个目标包含：**目标 / 现状 / 验收标准(DoD) / 关键文件 / 依赖 / 风险与对策 / 待决策 / 完成记录**。
> 完成项：勾选 `[x]`，并补一行 `- 完成: <hash> <日期> 备注`。
>
> **已锁定决策**（用户 2026-08-19 明确）：音乐=SMTC+音频回环；AI兼容=深度接入真实状态与审批；i18n=全应用双语。这三项都比最低成本方案重，工程量与风险已在对应条目如实标出。

---

## 执行顺序与依赖

```
G2(状态机修正) → G1(审批自动关)  → G3(i18n基建) → G5(水滴吸附) → G4(AI深接+图标) → G6(音乐) → G7(性能) → G8(提交+review)
    小/低风险       小/低风险        中/先建供复用    中          大(依赖1/2)        最大             全量            收尾
```

**建议每目标独立 commit**，G1~G3 完成后设一个审视点；**G4 先做"单工具端到端模板"并停下让你确认**，再决定是否铺开到其余 5 个工具（G4 工作量极大，需分批）。
G3 的 i18n 基建**优先建**——这样 G4 的 AI 图标窗口、G6 的音乐窗口从一开始就按双语构建，避免二次改造。

---

## G1. AI 权限卡片跨来源审批后不自动关闭

- **目标**：在其他地方（如 Claude Code 原生终端）已审批某 pending 权限后，悬浮岛的审批卡片自动关闭。
- **现状**：`pendingPermissions` 是 `Map`（key 为 `sessionId`）；hook 与原生 cmd 是 `Promise.race`，谁先到谁生效；cancel 只在 `sessionId` 匹配当前展示卡片时才隐藏卡（见 memory：`claude-code-permission-race-architecture`、`ai-island-permission-cross-session-fix`）。
- **验收标准(DoD)**：
  - [ ] 在任一来源审批一个 pending 权限后，悬浮岛对应卡片 ≤2s 自动关闭，无「幽灵卡片」残留。
  - [ ] 多会话时，仅关闭**对应 sessionId** 的卡片，不误伤其他会话的卡片。
- **关键文件**：`agent-state-machine.ts`、`agent-bridge.ts`、`ai-island.ts`、`ipc-handlers.ts`
- **依赖**：无（可与 G2 并行）
- **风险与对策**：
  - 风险：若审批完全发生在 hook 覆盖不到的路径，悬浮岛收不到"已处理"事件 → 卡片不关。
  - 对策：以被动 hook 事件为主，叠加**惰性对账**——下一次任何状态/会话事件到来时，把已非 pending 的 sessionId 卡片收起。
- **待决策**：兜底策略选「仅事件驱动」还是「事件+惰性对账」→ 建议后者（成本低、根治残留）。
- **完成**：核心（外部完成自动关闭）已在更早的 6332e68 落地——cardQueue FIFO + 完成事件（PostToolUse/PostToolUseFailure/PermissionDenied）经 session+tool_use_id/内容签名匹配对应权限卡并 reject 收起，多会话按 session+签名精确隔离。本次按建议补齐「惰性对账」最后一级：签名对不上时若 session 恰有一张同名卡则按名收起（Claude 工具串行 → 完成事件到达即证明该 gate 已 settle，安全）；多张同名卡签名都不对则不去重交 120s 超时。`test-permission-match.mjs` 通过。
- 完成: 8c76c4b 2026-08-19 惰性对账兜底（核心 6332e68 前置完成）

---

## G2. 多会话时"AI 待机中"误判

- **目标**：多个会话并行时，只要任一会话仍在工作，悬浮岛就显示对应工作状态；仅**全部** idle 才显示"AI 待机中"。
- **现状**：`resolveDisplayState()` 已经是"取所有会话最大优先级"（[agent-state-machine.ts:110-117](electron/main/agent-state-machine.ts#L110-L118)），理论上不该误判 → **根因未定，需先定位**。
- **疑似根因（按概率排，需验证）**：
  - (a) 展示层缓存了单一状态，未随 state 更新重算；
  - (b) `WORKING_STALE_MS`（5min）把"仍在思考但 >5min 无事件"的会话降级为 idle（[141-145](electron/main/agent-state-machine.ts#L141-L145)）——长思考易触发；
  - (c) 会话被 `sessions.delete` 误删，或 `sessionId` 复用覆盖了仍在工作的会话。
- **验收标准(DoD)**：
  - [ ] 构造可复现用例：2 个会话并行，其一结束、另一继续工作 >5min → 悬浮岛保持"工作中"，不显示待机。
  - [ ] 真实僵尸会话（无任何活动）仍能被 5min stale 回收。
- **关键文件**：`agent-state-machine.ts`、`ai-island.ts`
- **依赖**：无（先于 G4，G4 的状态机要基于修正后的逻辑）
- **风险与对策**：
  - 对策：若根因是 (b) stale 误伤，需区分"有活跃输出/思考的会话"与"静默僵尸"——不能粗暴调大 stale 而牺牲僵尸回收。可引入会话最近活动 heartbeat 或按会话类型区分 stale。
- **待决策**：stale 宽限策略（统一 5min vs 按会话类型/活动区分）→ 建议后者，需在定位根因后定。
- **完成**：根因确认是 (b) —— `WORKING_STALE_MS` 把静默 >5min 的活跃会话误降级 idle。修复 = 状态机注入 `isClaudeRunning`（复用 tasklist 缓存）区分活跃会话与真僵尸；claude 存活时非 idle 不降级，claude 全退出时按 5min 回收。日志改注入式，新增 `test-state-machine.mjs` 单测通过。**残余边角**：某会话的 claude 进程已死、但另一 claude 仍存活时，该僵尸保持 working 直到全部 claude 退出（比误判活跃会话为待机更符合预期）。
- 完成: 373e0c9 2026-08-19 状态机 liveness 旁证 + 单测

---

## G3. 设置窗口新增"中英文切换"（全应用双语）

- **目标**：悬浮球**设置窗口**可切换 中/英，全应用双语生效。
- **现状**：**无任何 i18n 基建**；字符串硬编码中文，分布在渲染层 `src/**/*.vue` **和**主进程多条内联 HTML 覆盖窗口（`floating-ball.ts`、`region-selector.ts`、`ai-island.ts`）。
- **验收标准(DoD)**：
  - [ ] 建立 i18n 层（zh/en 词条表 + 运行时切换 + 偏好持久化，写到 `floating-ball-settings.json`）。
  - [ ] 覆盖渲染层所有 `.vue`（HomeView/AiView/设置面板/待办）+ 主进程所有内联 HTML 覆盖窗口（悬浮球菜单/选区/AI岛/音乐窗口）。
  - [ ] 切换语言后全局即时生效（或统一"下次打开窗口生效"策略），且**新增窗口默认双语**。
- **关键文件**：新增 `i18n` 模块（可纯 Node 测，参考 `conversion-registry.ts` 范式）；`src/**/*.vue`；`electron/main/floating-ball.ts|region-selector.ts|ai-island.ts` 的内联 HTML；`preload/index.ts`（暴露 locale API），`ipc-handlers.ts`（广播 locale 变更）。
- **依赖**：建议优先完成，为 G4/G6 的新窗口双语打底。
- **风险与对策**：主进程内联 HTML 词条量大、需通知所有已 open 覆盖窗口 rebuild。对策：先词条化**UI 文案**，动态数据（歌名/会话内容）不动；切换即时性先在设置窗口自身验证。
- **待决策**：切换即时性（实时 rebuild 所有窗口 vs 下次打开生效）→ 建议后者降低复杂度。
- **完成**：

---

## G4. AI 兼容 Hermes / Cline / grok / Gemini-CLI / OpenCode / Codex + 图标

> **范围已锁定为"深度接入真实状态与审批"**——注意：这是几个工具里成本最高、最易碎的方案，多数工具（尤其 Cline）没有 Claude Code 那套 hooks。

- **目标**：悬浮球/AI助手 识别以上 6 个工具，展示其**真实会话状态**并支持审批；AI助手窗口展示**当前支持的 AI 图标**。
- **现状**：AI 子系统完全基于 Claude Code 专属 hooks + 权限 JSON 格式（`clawd-hook.js`、`agent-bridge.ts`、`agent-state-machine.ts`）。各新工具机制差异巨大：Codex/Gemini-CLI/Grok/OpenCode/Hermes 各有 CLI 钩子/会话文件；**Cline 是 VS Code 扩展、无 CLI hooks**，需走 workspace/会话文件或扩展事件，属特例。
- **验收标准(DoD)**：
  - [ ] 设计统一**工具适配器**接口（探测进程 → 建立会话 → 上报状态 → 产生/消费审批）。
  - [ ] 选 **1 个参考工具**做状态+审批端到端模板（建议 **OpenCode** 或 **Codex**，开源自适应强），**完成后停下让用户确认**再铺开。
  - [ ] 其余 5 工具逐个接入；Cline 的特例方案成文。
  - [ ] AI 助手窗口展示各工具图标 + 各自状态；悬空状态（该工具未接入审批时）明确降级展示。
- **关键文件**：新增 `agent-tools/` 适配器目录；`agent-state-machine.ts`、`agent-bridge.ts`、`ai-island.ts`、`ipc-handlers.ts`
- **依赖**：G2（状态机逻辑修正）、G3（窗口双语）
- **风险与对策（高）**：
  - **审批不可得**：多数工具**没有可注入的 hooks** → "深度审批"对它们可能物理上不可行。对策：状态深度接入、审批仅对具备 hook 机制的工具开放，其余明确降级并在 DoD 里成文（**这是最大风险，铺开前务必确认**）。
  - 协议不稳定/随版本失效：适配器做隔离，单工具失败不拖垮整体。
  - Cline 无 CLI 钩子：列为特例项，单独设计。
  - **安全暴露面扩大**：AI 岛是受信任覆盖层（`nodeIntegration:true`），新增多工具审批入口会放大本地权限面——外部/其他工具可能借此伪造审批。对策：审批做**来源/作用域校验**，新增工具接入前过一遍安全收敛，凭证与 sessionId 关联校验。
- **待决策**：① 无 hook 工具的"审批"如何处理（降级展示 vs 硬接易碎）→ 建议降级；② 图标资源来源/许可；③ 首个参考工具（建议 OpenCode 或 Codex）。
- **完成**：

---

## G5. 悬浮球屏幕边缘水滴吸附

- **目标**：悬浮球靠近屏幕边缘时自动吸附，视觉呈"水滴附着在边缘"的圆润贴合 + 弹性回弹。
- **现状**：悬浮球由 `floating-ball.ts` 管理，位置持久化到 `floating-ball-pos.json`。
- **验收标准(DoD)**：
  - [ ] 距边缘 ≤ 阈值自动吸附（贴边圆角贴合，可自定义留白像素）。
  - [ ] 吸附有弹性过渡动画，拖动可平滑脱离；位置持久化包含吸附状态。
  - [ ] 多显示器边界、贴边区域点击穿透、与 always-on-top 无冲突。
- **关键文件**：`floating-ball.ts`
- **依赖**：无
- **风险与对策**：与现有拖动/位置持久化逻辑冲突（需保留用户最近放置位置）；贴边区域 hit-test 需处理穿透；回弹动画别造成高频 `setBounds`（参考 `a8d8389` 的 NaN 防御）。对策：吸附仅在"松手后静止靠近边缘"时触发，拖动中不强拉。
- **待决策**：吸附阈值（px）、贴边留白（建议 ≈0 全贴合）、是否四边吸附。
- **完成**：

---

## G6. 音乐功能：跨软件兼容 + 桌面歌词 + 炫酷指示器

> **已锁定：SMTC（系统媒体控件）+ 音频回环**。全项目风险最高、工程量最大的一项，且**当前音乐窗口完全不存在**（"音乐"花瓣目前走 `else` 空分支，见 [floating-ball.ts:388-399](electron/main/floating-ball.ts#L388-L399)）。

- **目标**：悬浮球音乐窗口兼容 QQ音乐/网易云/酷狗/汽水音乐等**所有已注册到系统的播放器**；UI 与其他窗口统一风格；支持**桌面歌词**与**炫酷音乐指示器**。
- **建议拆分为子任务**（便于分批交付与风险隔离）：
  - **6a 基础信息+控制**：SMTC 取 歌名/歌手/封面 + 播放/暂停/上一首/下一首。
  - **6b 桌面歌词**：按 歌名/歌手 抓取 LRC → 用 SMTC 播放位置（或音频对齐）同步跟唱。
  - **6c 炫酷指示器**：系统音频**回环采集**（WASAPI loopback）→ 频谱可视化。
  - **6d 音乐窗口 UI**：与全应用统一风格，双语（依赖 G3）。
- **验收标准(DoD)**：
  - [ ] 启动任一受支持播放器 → 音乐窗口显示歌名/歌手/封面并可控制。
  - [ ] 桌面歌词可开/关、可跟唱。（6b）
  - [ ] 指示器随音乐实时动态。（6c）
  - [ ] 窗口风格与其余覆盖窗口一致且双语。（6d）
- **关键文件**：新增 `electron/main/music-*.ts` 子系统；渲染层 `MusicView`；`floating-ball.ts`（接音乐花瓣）
- **依赖**：G3（双语）
- **风险与对策（最高）**：
  - **SMTC 调用载体**：需原生能力（Node 原生模块 / edge / 或经 ffmpeg/辅助进程）——需先做技术验证（SPIKE），失败则退化为系统快捷键/轮询。
  - **音频回环**：WASAPI loopback 非默认可用，需原生实现或可靠 dshow 设备枚举；权限/驱动敏感——先验证可行性。
  - **歌词数据源**：LRC 抓取稳定性/版权受限 → 需定数据源策略。
  - **与录屏采集冲突**：指示器的音频回环采集可能与录制子系统对系统音频的采集/电平侦测**竞争资源**（两者同采系统音频）。对策：采集策略复用/共享，避免两路同时高频占用；先确认录制与音乐回环能否并存。
  - **对策**：先做 6a SPIKE + 最小可用闭环，验证 SMTC 与回环在本机可跑通，再铺开 6b/6c。
- **待决策**：① SMTC 实现载体（原生模块 vs 辅助进程）→ SPIKE 后定；② 音频回环实现路径 → SPIKE 后定；③ 歌词数据源；④ 指示器视觉形态。
- **完成**：

---

## G7. 整体性能优化

- **目标**：全部功能完成后，围绕 轻量化 / 高性能 / 资源及时释放 / 各类资源占用下降 优化。
- **现状**：已有 `docs/superpowers/specs/2026-07-30-performance-audit.md`，其中部分建议已落地（ffmpeg kill 注册表、硬件编码、串行合并、rVFC、local-video://、app-before-quit 广播）。**先查代码再假定审计项未修**。
- **验收标准(DoD)**：
  - [ ] 建立**可测 KPI**（内存 / CPU / 句柄 / 子进程 / 定时器/监听器泄漏），优化前后量化对比。
  - [ ] 重点清理：在途 ffmpeg 子进程、定时器、事件监听器、覆盖窗口、HTTP server —— 新资源一律接入 `before-quit` 清理咽喉。
  - [ ] **把 G4/G6 新增的持续占用一并纳入审计**（工具探测轮询、SMTC/歌词跟唱回调、音频频谱采集），避免新功能把轻量化目标冲回原点。
  - [ ] 回归验证：录制、覆盖窗口、AI 子系统在优化后**无功能/性能退化**。
- **关键文件**：`index.ts`(before-quit) / `conversion-registry.ts` / `hw-encoder.ts` / 全量
- **依赖**：G1~G6 之后
- **风险与对策**：不得触碰承重约束（如 `backgroundThrottling:false` 对录制、串行合并勿改并行、绘制循环的 rVFC 门控）；任何"性能修复"以可测数据为准，不凭直觉下改。
  - **跨目标张力**：音乐频谱/歌词这类高频渲染窗口若被背景节流会影响体验，但**不能为迁就它而无差别改节流策略**（违反承重约束）——需在"保持节流"与"音乐窗口不卡顿"间取平衡（如仅对该覆盖窗口单独豁免，且不波及主窗口录制）。
- **KPI 建议**：以优化前基线为参照，记录 内存 / CPU(空闲与活跃) / 句柄数 / 子进程数 / 常驻定时器与监听器 的量化对比。
- **完成**：

---

## G8. 提交全部修改 + 最终 Review

- **目标**：所有目标完成后，推送所有修改到仓库，做最终整体 review。
- **验收标准(DoD)**：
  - [ ] 全部目标对应提交已 **push** 到远端 `main`（仓库 `MUERZHI-Multi-functional-AI-Floating-Ball`）。
  - [ ] **提交作者只署名本人（muerzhi）**，不加 AI 协作者署名（见 memory：`commit-author-only-user`）。
  - [ ] 最终 review：代码正确性 + 性能 + 资源占用，结论与遗留备注记录。
- **依赖**：G1~G7
- **完成**：

---

## 风险总览（优先级排序）

| 风险 | 涉及 | 等级 | 对策 |
|---|---|---|---|
| G4 对多数工具"深度审批"物理上不可行 | G4 | 🔴 高 | 铺开前先做单工具模板并停下确认；审批仅对具 hook 机制的工具开放，其余降级成文 |
| G6 SMTC / 音频回环 原生可行性未验证 | G6 | 🔴 高 | 先做 SPIKE + 6a 最小闭环，验证通过再铺开 |
| G4 审批暴露面扩大（外部工具可伪造审批） | G4 | 🟠 中 | 审批做来源/作用域校验；工具接入前安全收敛 |
| G6 音频回环与录屏采集资源竞争 | G6 | 🟠 中 | 采集策略复用/共享，避免双路高频占用 |
| G4/G6 新增持续占用冲掉 G7 轻量化 | G4/G6/G7 | 🟠 中 | G7 审计须覆盖新增功能的持续占用（探测轮询/歌词/频谱） |
| G2 根因未定（疑似 stale 误伤活跃会话） | G2 | 🟠 中 | 先定位根因再改，别粗暴调 stale |
| G3 主进程内联 HTML 双语重构量大 | G3 | 🟠 中 | 先词条化 UI 文案；切换即时性先设"下次打开生效" |
| 覆盖窗口节流 vs 音乐高频渲染张力 | G6/G7 | 🟡 低 | 仅对音乐窗口单独豁免，不波及主窗口录制节流 |
| G5 与拖动/位置持久化冲突 | G5 | 🟡 低 | 仅静止近边触发吸附，保留放置位置 |
